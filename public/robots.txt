User-agent: *
Allow: /

# Exclude potential sensitive or irrelevant directories if they ever exist
Disallow: /api/
Disallow: /admin/

# /llms.txt is intentionally left crawlable — it's the AI-facing index of the
# site and must be fetchable by AI tools and crawlers.

# skill.md is an agent-action endpoint, not content for search indexing.
Disallow: /skill.md

Sitemap: https://learnhns.com/sitemap-index.xml
